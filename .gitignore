# Gradle / build
.gradle/
build/
app/build/

# Android Studio / IntelliJ
.idea/
*.iml
.DS_Store
captures/

# Local config / secrets
local.properties

# Signing keys
*.jks
*.keystore

# Firebase — contains project identifiers; keep out of the repo per project decision
google-services.json
app/google-services.json
